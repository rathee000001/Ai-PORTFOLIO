import './homeIdentity.css';
export default function HomeIdentity({still}:{still:boolean}){return <div className={`home-identity ${still?'home-identity-still':''}`}><a href="/" className="gate-name" aria-label="Praveen Rathee — Home"><span className="identity-lettering" aria-hidden="true">{Array.from('Praveen Rathee').map((letter,index)=><i key={index} style={{animationDelay:`${index*-.22}s`}}>{letter===' '?'\u00a0':letter}</i>)}</span><span className="identity-subtitle">BUSINESS ANALYSIS · ANALYTICS · AI WORKFLOWS</span></a><span className="avatar-track" aria-hidden="true"><span className="home-avatar"/></span></div>;}
