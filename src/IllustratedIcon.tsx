import type {CSSProperties} from 'react';
import './illustratedIcon.css';
export function illustrationIndex(name:string){if(/govern|security|permission|grant|accept|oversight/i.test(name))return 1;if(/api|mcp|sdk|http|connector|transport|request/i.test(name))return 2;if(/excel|spreadsheet|workbook|table|pivot|lookup|formula|csv|field/i.test(name))return 3;if(/forecast|regression|arima|statsmodels|xgboost|neural|model|quantile|coefficient|feature/i.test(name))return 4;if(/sql|database|store|index|registry/i.test(name))return 5;if(/pipeline|integration|ingestion|etl|normaliz|transform/i.test(name))return 6;if(/code|python|react|typescript|javascript|html|css|rust|build|developer/i.test(name))return 7;if(/document|research|source|file|pdf|ocr|parse|report|guide|extract/i.test(name))return 8;if(/stakeholder|audience|team|collabor|employee|investor|people/i.test(name))return 9;if(/supply|cargo|delivery|ship|logistic|inventory|procure|warehouse/i.test(name))return 10;if(/financ|cost|ratio|retail|profit|margin|pricing/i.test(name))return 11;if(/workflow|process|operation|plan|task|continuity/i.test(name))return 12;if(/present|visual|dashboard|chart|kpi/i.test(name))return 13;if(/decision|strategy|scenario|position|requirement|business/i.test(name))return 14;if(/valid|quality|check|review|test|error|evaluation/i.test(name))return 15;return 0;}
export default function IllustratedIcon({name,index,size=32,sheet='skills'}:{name?:string;index?:number;size?:number;sheet?:'skills'|'domain'}){const frame=index??illustrationIndex(name||'analysis');return <span className="illustrated-icon" aria-hidden="true" style={{width:size,height:size,backgroundImage:`url('/worlds/${sheet==='domain'?'domain':'skill'}-illustrations.png')`,'--illustration-x':`${frame%4/3*100}%`,'--illustration-y':`${Math.floor(frame/4)/3*100}%`} as CSSProperties}/>;}
