import {artifactDefinition} from './SemanticArtifact';
import type {ArtifactVisual} from './types';
export default function ArtifactFacts({visual,definition}:{visual?:string;definition?:ArtifactVisual}){const d=definition||artifactDefinition(visual);if(!d||!d.content.length||['world','sprite','orb','code'].includes(d.type))return null;const rows=d.content.filter(Array.isArray) as (string|number)[][];return <div className="artifact-facts"><strong>{d.title}</strong>{d.headers&&rows.length?<table><thead><tr>{d.headers.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={i}>{row.map((cell,j)=><td key={j}>{cell}</td>)}</tr>)}</tbody></table>:<ul>{d.content.map((item,i)=><li key={i}>{Array.isArray(item)?item.join(' · '):item}</li>)}</ul>}{d.caption?<small>{d.caption}</small>:null}</div>;}
